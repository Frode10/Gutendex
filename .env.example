# Set to 'true' to use mock data instead of real API
# Useful for development when the Gutendex API is slow or unavailable
VITE_USE_MOCK=true

# For production, set to 'false' to use the real API
# VITE_USE_MOCK=false
